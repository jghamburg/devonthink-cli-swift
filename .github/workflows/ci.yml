name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  build:
    name: Build (macOS arm64)
    runs-on: macos-15        # arm64, Xcode 16, Swift 6 pre-installed — no setup action needed

    steps:
      - uses: actions/checkout@v4

      - name: Build
        run: swift build
